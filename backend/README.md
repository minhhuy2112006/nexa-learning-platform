# Backend

This folder is reserved for the NEXA API, authentication, database integration, and background services.

Keeping it separate from `frontend/` lets the product share one repository while deploying the interface and backend independently.
