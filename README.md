# NEXA Learning Platform

Monorepo for the NEXA learning platform.

- `frontend/` — static student-facing interface, published through GitHub Pages.
- `backend/` — reserved for the future API, authentication, and data layer.

## Run the frontend locally

```powershell
cd frontend
node serve.js
```

The public frontend is deployed from `frontend/` by GitHub Actions.
